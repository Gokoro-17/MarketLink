# MarketLink backend
Express API starter with security headers, CORS, JSON parsing, and a health endpoint.

Run `npm install`, copy `.env.example` to `.env`, then use `npm run dev`. The health check is `GET /api/health`. Database connectivity and authentication are not configured yet.
