# MarketLink frontend
React and Vite starter for the MarketLink web client.

Run locally with `npm install` and `npm run dev`. The app is served at http://localhost:5173. Set `VITE_API_URL` to override the API URL (defaults to http://localhost:4000/api).
